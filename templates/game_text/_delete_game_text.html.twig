{{ include('default/_delete_post_confirmation.html.twig') }}
<form action="/admin/delete/game-text/{{ id }}" method="post" data-confirmation="true" id="delete-form">
    <input type="hidden" name="token" value="{{ csrf_token('delete') }}" />
    <button type="submit" class="btn btn-lg btn-block btn-danger button">
        <i class="fa fa-trash" aria-hidden="true"></i>
        Esborrar
    </button>
</form>